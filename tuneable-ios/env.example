# Tuneable iOS – config checklist
# The app reads TUNEABLE_API_URL from Info.plist (optional).
# The backend must be running and reachable at that URL.

# -----------------------------------------------------------------------------
# Backend (tuneable-backend) must have these set for the API to work:
# -----------------------------------------------------------------------------
# MONGODB_URI=...
# JWT_SECRET=...
# PORT=8000

# For login and profile:
# - User model and /users/login, /users/profile must be available

# For parties:
# - Party routes and Socket.IO (optional for real-time) on same host/port

# -----------------------------------------------------------------------------
# iOS app (Xcode / Info.plist)
# -----------------------------------------------------------------------------
# TUNEABLE_API_URL = base URL only, e.g.:
#   Simulator:  http://localhost:8000
#   Device:     http://YOUR_MAC_IP:8000
#   Production: https://api.tuneable.stream
